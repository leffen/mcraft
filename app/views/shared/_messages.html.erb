<% if flash.count > 0 %>
    <div class="content top_container">
      <div class="row">
        <div class="span12">
          <% flash.each do |type, message| %>
              <div class="alert <%= bootstrap_class_for(type) %> fade in">
                <button class="close" data-dismiss="alert">×</button>
                <%= message %>
              </div>
          <% end %>

        </div>

      </div>
    </div>
<% end %>

