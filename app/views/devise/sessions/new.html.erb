<div class="container top_container">
  <div class="row">
    <div class="span6">


<h2>Logg på</h2>

<%= simple_form_for(resource, :as => resource_name, :url => session_path(resource_name),:html => {:class => 'form-horizontal' }) do |f| %>
  <div class="control-group">
    <%= f.input :username, :required => false, :autofocus => true, :label => 'Brukernavn' %>
    <%= f.input :password, :required => false, :label => 'Passord' %>
    <%= f.input :remember_me,:label => 'Husk meg', :as => :boolean if devise_mapping.rememberable? %>
    <%= f.button :submit, "Login", :class => 'btn-large btn-primary' %>
  </div>


<% end %>

<%= render "devise/shared/links" %>


    </div>
  </div>
</div>
